{
  "name": "covid-tracker",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "install-client": "cd covid-logs && npm install",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix covid-logs && npm run build --prefix covid-logs",
    "start": "node server.js",
    "nodemon": "nodemon server.js",
    "client": "npm start --prefix covid-logs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
